doctype html
html(lang="ko")
    head
        title #{pageTitle} | Wetube
        link(rel="stylesheet" href="https://unpkg.com/mvp.css")
    body
      header 
        if fakeUser.loggedIn
          small Hello #{fakeUser.username}
        nav
          ul
            if fakeUser.loggedIn
              li
                a(href ="/logout") log out
            else
              li
                a(href ="/login") login
              
        h1=pageTitle  
        //-#{pageTitle}과 같은 역할이지만 여기서는 다른 Text와 섞어 쓰지 않기 때문#{pageTitle}과 같은 역할이지만 여기서는 다른 Text와 섞어 쓰지 않기 때문
          하나의 variable만 사용하는 경우라면 이런 식으로 사용함
      main
        block content
    include partials/footer.pug